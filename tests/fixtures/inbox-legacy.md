# Inbox — legacy prose only (no structured fields)

## Future Handoff Prompt

```
TASK: Example legacy handoff
Preferred implementer: Windsurf Cascade

EXECUTION RULES:
- Stage only README.md. Never use git add .
- Push origin main after commit (authorized — push is explicitly authorized).
```
